# QA (ユーザー確認事項 一覧)

ユーザーの判断を待っている項目。回答を書き込むか、manager に口頭で伝えていただければ反映します。

---

## 1. check_equivalence テスト仕様の承認 (Task #5)

- 仕様書: `lee/test-check_equivalence-spec.md`
- 直近の更新: A-RANDOM (シフトベース正例) + B-RANDOM (x/y スワップによる負例) 追加済
- 決定論 / ランダム両方のケースあり

**Q1.** 現内容で承認しますか?
→ 回答: ok (**解決済** — 2026-04-17 tester Task #5 を completed とし #6 に着手)

---

## 2. insert_map テスト仕様の確認事項 3 件 (Task #8)

- 仕様書: `lee/test-insert_map-spec.md`

### Q2-1. テスト規模

- map サイズ: **W=10, H=10**
- 反復回数: **N=50**
- このデフォルトで OK か、別の値がよいか?

→ 回答:OK

### Q2-2. 「1行+1列挿入」の適用方法

- plan.md の記述: 「sとtの間にランダムに1行の挿入と1列の挿入を行う」
- (A) **連続適用**: 1行挿入 → 続けて1列挿入 (座標は挿入を受けてシフト済みの状態で行う)
- (B) **独立2ケース**: 1反復で片方だけ、別反復でもう片方

現在の仕様は (A) 連続。どちらを採用しますか?
→ 回答:(A)続けて挿入。さらに修正して10行と10列の挿入にしました。このケースを10ケース行って。

### Q2-3. 決定論的ケース D1–D7 [再質問・説明追加]

**文脈**: これは **insert_map のテスト仕様** (`lee/test-insert_map-spec.md`) の話です。
ランダムテストとは別に、tester が「回帰用に必ず通しておきたい固定パターン」として 7 ケースを提案しています。`lee/test-insert_map-spec.md` の 49–59 行目にある表で、内容は:

- **D1** 水平線の途中に列挿入 → 線が伸びる
- **D2** 垂直線の途中に行挿入 → 線が伸びる
- **D3** L字 ('┌' + '─') の間に列挿入 → 横方向接続が埋まる
- **D4** 十字 ('┼') 付近に列挿入 → 横線延長、縦線維持
- **D5** 空 map に挿入 → 何も起きず terminal 座標だけシフト
- **D6** terminal の直後に列挿入 → terminal 位置不変、空列追加
- **D7** `y=0` に行挿入 → 全 terminal の y が +1

**Q**: これらを含めるか、スキップしてランダムテストのみにするか?

→ 回答: それらも含めます。D1-D4 はもっと網羅的に全9種類のsubportの隣接する4パターンすべてに全9種類のsubportがあり、その間に行または列を挿入するケースを全て調べて。ただし、サブポートが繋がっていないケース '-|'や'-L'や'||'や'  'の間は' '以外を挿入するとＮＧとします。

### Q2-3-fup. 「隣接する4パターン」の意味 [再質問]

9種類 × 9種類 = 81 の subport ペアについて、以下どの 4 パターンを指していますか?

- (α) **挿入の向き × 隣接の向き の組合せ** (4通り):
  1. 横並び (A-B) に **列挿入** (A と B の間)
  2. 横並び (A-B) に **行挿入** (同じ y に空行を挟む)
  3. 縦並び (A/B) に **列挿入** (同じ x に空列を挟む)
  4. 縦並び (A/B) に **行挿入** (A と B の間)

- (β) **隣接方向のみ** (4通り、挿入は自動決定):
  1. A の右に B (col挿入で分離)
  2. A の下に B (row挿入で分離)
  3. A の左に B (同 1 と同じ形、順序だけ逆)
  4. A の上に B (同 2 と同じ形、順序だけ逆)

- (γ) 

合計ケース数: (α) なら 81 × 4 = **324ケース**, (β) なら 81 × 2 = **162ケース** (順序反転を別扱いしないなら)

→ 回答:subport 1 の {上,下,左,右} に subport 2 がある 4 パターン. 9x4x9 ですね (**解決済** — 2026-04-17 tester: 324 ケース仕様を `lee/test-insert_map-spec.md` の D1–D4 拡張として設計中)

---

## 3. insert_map の 5 引数拡張の是非

coder が `insert_map(m, x, y, dir, T?)` の 5 引数に拡張しました (plan.md の 4 引数から拡張、T はオプショナル)。

- T を渡すと terminal 座標を in-place でシフト
- T 内の 'T' セルの dx/dy を考慮して seam にブリッジ subport を配置

check_equivalence テストでは **使わない** 方針に変更したので、insert_map テストのみで使われる想定。

**Q3.** この拡張を許可しますか? plan.md の仕様も更新しますか?
→ 回答:それで正しいです。もっと言うとポートPもいりますね。

### Q3-fup. ポート P も渡す拡張 [再質問]

確認: `insert_map(m, x, y, dir, T, P?)` のように **P (portリスト) も受け取る** 拡張をしますか?

- P は `{s, e}` の形で terminal のインデックスを持つだけなので、座標を持っていない
- したがって挿入で座標シフトの影響は受けない
- P を渡す目的は何ですか? 以下のどれに該当しますか (または他)?
  - (a) seam に橋渡しする subport を選ぶとき、**「どの port に属する線か」** を考慮して ┼ や L字など複雑な形を選ぶ
  - (b) 挿入が **特定の port を切断する** 場合にエラーを返す
  - (c) 挿入後の port 情報をログ出力用に受け取るだけ (内部では不使用)
  - (d) その他 (教えてください)

→ 回答: そうですね。拡張いりませんでした。

### Q3-fup-2. (a)〜(d) のどれ? [再質問]

「yes」だけだと P を渡す目的が分からないので (a)/(b)/(c)/(d) のいずれかを教えてください。特に (a) なら seam の subport 選択アルゴリズムを拡張する必要があり、(c) なら単にシグネチャに追加するだけで済みます。

→ 回答: 書き直しました。

---

## 4. insert_map テストの挿入順序

tester は「10行挿入 → 10列挿入」の順で実装しました。plan.md は「ランダムに10行の挿入と10列の挿入」で順序は未指定。どれにしますか?

- (a) 10行 → 10列 (tester 現仕様)
- (b) 10列 → 10行 (逆順)
- (c) 20回をランダム順 (毎回 row/col をランダム選択)

→ 回答: (1行 -> 1列) を10回

---

## 5. その他 新規に発生した質問があればここに追記

（manager が随時追加予定）
